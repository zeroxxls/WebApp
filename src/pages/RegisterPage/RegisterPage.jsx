import React from 'react';
import { RegisterSection } from '../../modules/auth';

export const RegisterPage = () => {
    return (
        <div>
            <RegisterSection/>
        </div>
    );
};